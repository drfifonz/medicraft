general:
  experiment_id: phd000-test
  total_steps: 1
  image_size: [256,512]
  spot_checkpointing: True
  models:
    unet:
      dim: 64
      dim_mults: [1, 2, 4, 8]
      channels: 1

    diffusion:
      timesteps: 1000




data:
  validation_split: 0.1 / 0.9
  csv_file_path: data/datasets/ophthal_anonym/dataset-splitted.csv
  split_seed: 43

experiment:
  loop:
    - name: foo
      foo: foo
    ### reference ###
    - name: generate_samples
      repeat: false
      relative_dataset_results_dir: datasets/phd/0000p2
      class_name: reference
      num_samples: 5000
      batch_size: 10
      checkpoint: .results/reference/model-25.pt
      # use_wandb: false
      step_job_id: 1
    ### fluid ###
    - name: generate_samples
      repeat: false
      relative_dataset_results_dir: datasets/phd/0000p2
      class_name: fluid
      num_samples: 5000
      batch_size: 10
      checkpoint: .results/fluid/latest.pt
      # use_wandb: false
      step_job_id: 2
    ### benign ###
    - name: generate_samples
      repeat: false
      relative_dataset_results_dir: datasets/phd/0000p2
      class_name: benign
      num_samples: 5000
      batch_size: 10
      checkpoint: .results/benign/latest.pt
      # use_wandb: false
      step_job_id: 3
    ### precancerous ###
    - name: generate_samples
      repeat: false
      relative_dataset_results_dir: datasets/phd/0000p2
      class_name: precancerous
      num_samples: 5000
      batch_size: 10
      checkpoint: .results/precancerous/latest.pt
      # use_wandb: false
      step_job_id: 4


output:
  results_dir : .results
  # copy_results_to: '~OneDrive/.......'
